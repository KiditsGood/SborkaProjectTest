{% macro menu(active=false) %}
	<nav class="header__nav">
		{% for item in menuItems %}
			<a href="{{ item.path }}" class="header__nav-item{% if item.id == activeItem %} _active{% endif %}" style="margin-right: 15px;">{{ item.name }}</a>
		{% endfor %}
	</nav>
{% endmacro %}
