{% macro menu(active=false) %}
	<nav class="header__nav">
		{% for item in menuItems %}
			<a href="{{ item.path }}" class="header__nav-item">{{ item.name }}</a>
		{% endfor %}
	</nav>
{% endmacro %}
